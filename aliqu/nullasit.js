// Declare a variable with let (block-scoped)
let myVariable = 'Hello, World!';

// Declare a variable with var (function-scoped)
var myVariable = 'Hello, World!';

// Declare a constant
const myConstant = 42;
